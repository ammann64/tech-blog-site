{{#each posts as |post| }}

<div class="post">
    <div>
        <h2>
            <a href="/post/{{post.id}}">{{post.title}}, posted by {{post.user.name}}</a>
        </h2>
    </div>
</div>
{{/each}}